# This file was generated by the Gradle 'init' task.
# https://docs.gradle.org/current/userguide/build_environment.html#sec:gradle_configuration_properties

# GraalVM buildtools 0.10.x has a known incompatibility with Gradle 9's configuration
# cache (generateResourcesConfigFile serializes a non-cacheable Configuration type).
# Disable until a fixed plugin version is available.
org.gradle.configuration-cache=false

# Project version — update this before tagging a release (e.g. git tag v0.2.0)
version=0.1.0

